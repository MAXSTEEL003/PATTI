{
  "name": "patti-calculator-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "echo 'Ensure dependencies in src are installed when needed'",
    "build": "node scripts/build.js",
    "start": "cd src && npm run dev"
  }
}
