{
  "name": "patti-calculator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd src && http-server -c-1 . -p 8000",
    "start": "npm run dev",
    "build": "mkdir -p dist && cp -r src/* dist/ || true"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
